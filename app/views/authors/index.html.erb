<h1>Author list</h1>

<ul>

  <% @authors.each do |author| %>
  <li><%= author.id %> <%= author.name %> | <%= link_to 'edit', edit_author_path(author) %>
    <ul>
      <% author.books.each do |book| %>
      <li><%= book.id %> <%= book.title %> </li>
      <% end %>
    </ul>
</li>
  <% end %>
</ul>
  <%= link_to 'create new author', new_author_path %>
